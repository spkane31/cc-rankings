<h1>Listing Teams</h1>

<table>
  <%= for team <- @teams do %>
    <tr>
      <td><b><%= get_name(team) %></b> (<%= team.id %>)</td>
      <td><%= link "View", to: Routes.team_path(@conn, :show, team.id) %></td>
    </tr>
  <% end %>
</table>