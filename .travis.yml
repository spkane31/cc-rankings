language: elixir

elixir:
  - 1.8.2

addons:
  postgresql: '10.9'

service:
  - postgresql

before script:
  - cp config/travis.exs config/test.exs

script:
  - mix deps.get --only test
  - mix ecto.create
  - mix ecto.migrate
  - mix test